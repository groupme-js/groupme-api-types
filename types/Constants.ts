export enum MessageSenderType {
    Service = "service",
    System =  "system",
    User =    "user",
}

export enum MessageDeletionActor {
    Sender = "sender",
}

export enum MessageDeletionMode {
    Creator = "creator",
    Sender =  "sender"
}