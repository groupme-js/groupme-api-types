{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clean",
            "type": "shell",
            "command": [
                "rm */*/*.d.ts;",
                "rm */*/*.js;",
                "rm */*/*.d.ts.map;",
                "rm */*/*.js.map;",
                "rm */*.d.ts;",
                "rm */*.js;",
                "rm */*.d.ts.map;",
                "rm */*.js.map;",
                "rm *.d.ts;",
                "rm *.js;",
                "rm *.d.ts.map;",
                "rm *.js.map;"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}